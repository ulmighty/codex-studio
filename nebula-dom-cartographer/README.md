# Nebula DOM Cartographer

GPU-accelerated DOM analysis and AI-assisted selector generation.

## USAGE

```bash
npm install
npm run build
npm test
```

Load `dist` directory as an unpacked extension in Chrome.

## BENCHMARK_GUIDE

Run benchmarks with:

```bash
npm run bench
```
