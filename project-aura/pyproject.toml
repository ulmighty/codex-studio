[project]
name = "project-aura"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.9"
ignore_missing_imports = true
