# AGENTS Instructions for SentinelStack

- Run `pytest` before committing to ensure tests pass.
- Keep code PEP8-compliant and include type hints.
- The main application entry point is `src/core/main.py`. Run the app via:
  `python src/core/main.py`
- Tests live in `tests/` and should cover core logic and plugins.
